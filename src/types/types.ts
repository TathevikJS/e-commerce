export interface Product {
    id: string;
    title: string;
    product_type: string;
    price: string;
    image: string;
}

export interface User {
    username: string;
    about: string;
    image: string;
}